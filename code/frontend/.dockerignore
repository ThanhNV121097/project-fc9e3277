# node_modules is reinstalled from the lockfile inside the image; shipping the
# host's copy would both slow the build and risk carrying a native module built
# for the wrong architecture.
node_modules
.next
out
.env
.env.local
.git
Dockerfile
.dockerignore
npm-debug.log*
